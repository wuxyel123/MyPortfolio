.DS_Store

# Private job-search material — must never reach the FTP deploy
LinkedIn_Job_Search/

# Original vendor template archive, not part of the published site
OriginalTemplate.zip

# Local dev tooling — everything committed here gets FTP'd to the live site
.claude/
