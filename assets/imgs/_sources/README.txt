Original photos, kept under version control but NOT deployed to the web host
(see the `exclude` list in .github/workflows/main.yml).

  header.jpg        full-length original; portrait.webp/.jpg are cropped from it
  avatar.jpg        older head-and-shoulders crop, unused
  avatar_badge.*    background-removed cutout; avatar_head.jpg is cropped from it

Regenerate the derived images from these rather than editing the derived
files in place — they are the only copies.
